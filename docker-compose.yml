version: '3.8'
services:
  backend:
    build:
      context: ./backend
    ports:
      - "4000:4000"
    env_file:
      - .env.example
    command: npm run dev
  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    command: npm run dev
